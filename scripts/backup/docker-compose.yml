version: '3.8'

services:
  supabase-export:
    build: .
    environment:
      # Ganti dengan credentials Supabase Anda
      - SUPABASE_URL=https://emfvoassfrsokqwspuml.supabase.co
      - SUPABASE_ANON_KEY=YOUR_ANON_KEY_HERE
    volumes:
      # Output akan disimpan di folder ./output di host
      - ./output:/app/output
